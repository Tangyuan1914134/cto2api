version: '3.8'
services:
  web-proxy-api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - API_KEY=${API_KEY:-default-api-key}
      - BASE_URL=http://localhost:5000
      - PORT=5000
      - DEBUG=False
    volumes:
      - .:/app
